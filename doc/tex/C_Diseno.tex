\apendice{Especificación de diseño}

\section{Introducción}


\section{Diseño de datos}
El diseño de datos define la estructura de persistencia que soporta el funcionamiento de la aplicación, garantizando que la información se almacene de forma coherente, trazable y escalable. En este apartado se presenta el esquema lógico de la base de datos, con el objetivo de justificar las entidades principales del dominio, sus relaciones y las restricciones de integridad que permiten mantener consistencia entre usuarios, parcelas, imágenes procesadas y resultados generados por el sistema.

\begin{figure}[tbp]
	\centering
	\includegraphics[
	width=\textwidth,
	trim=0 670 0 0,
	clip
	]{img/Diagrama_E-R.pdf}
	\caption[Diagrama E-R de la base de datos]{Diagrama E-R de la base de datos.}
	\label{fig:esquemaerbbdd}
\end{figure}

\subsection{Modelo Entidad--Relación}
El diagrama general E/R se muestra en la Figura~\ref{fig:esquemaerbbdd}. Para el manejo de la información necesaria en el flujo de la aplicación se han definido las siguientes entidades, junto con sus relaciones y cardinalidades:

\begin{itemize}
	\item \textbf{Usuario:} representa a la persona registrada en la aplicación y constituye el origen del control de acceso. Incluye un identificador que actúa como clave primaria, así como atributos de autenticación y perfil, como el nombre de usuario, el correo electrónico, la contraseña, el rol y la fecha de alta. Un usuario puede crear múltiples parcelas, lo que queda reflejado mediante una relación uno a muchos.
	
	\item \textbf{Parcela:} representa una unidad de trabajo asociada a un usuario, entendida como el área seleccionada por el mismo sobre el que se pretende organizar la información de análisis. Dispone de un identificador como clave primaria y, además, almacena atributos espaciales y descriptivos, como el tamaño en metros, el punto de origen, el punto final y una fecha asociada a la parcela. Cada una de ellas puede generar un \textit{grid} con múltiples fotos, estableciendo de nuevo una relación uno a muchos.
	
	\item \textbf{Foto:} modela cada imagen procesada dentro de una parcela y concentra los metadatos necesarios para su trazabilidad. Incluye un identificador como clave primaria y atributos como fecha de captura, resolución expresada en valor y unidad, coordenadas geográficas y rutas de almacenamiento, diferenciando la ruta de la imagen original y la ruta del resultado de trazas. Una foto puede contener exactamente un conjunto de trazas asociado, formalizado mediante una relación uno a uno.
	
	\item \textbf{Trazas:} representa el resultado generado por el sistema a partir de una foto concreta. Contiene un identificador como clave primaria y una ruta asociada al recurso resultante. Cada uno de estos ficheros .json están asociados a una imagen, por ello, obtenemos la relación uno a uno.
\end{itemize}

\begin{figure}[tbp]
	\centering
	\includegraphics[
	width=\textwidth,
	trim=0 550 0 0,
	clip
	]{img/Esquema_relacional_BBDD.pdf}
	\caption[Diagrama relacional de la base de datos]{Diagrama relacional de la base de datos.}
	\label{fig:esquemarelacionalbbdd}
\end{figure}

En la Figura~\ref{fig:esquemarelacionalbbdd} se recoge la traducción del modelo conceptual al esquema relacional, estructurado en las tablas \textit{Usuario}, \textit{Parcela} y \textit{Foto}. La tabla \textit{Usuario} concentra los atributos de identificación y control de acceso, mientras que \textit{Parcela} actúa como entidad dependiente mediante la clave foránea \texttt{usuario\_id}, estableciendo una relación uno a muchos que permite organizar el histórico de trabajo por zonas o unidades de análisis. A su vez, la tabla \textit{Foto} se vincula a \textit{Parcela} a través de \texttt{parcela\_id}, almacenando tanto metadatos relevantes para caracterizar la imagen, como la fecha de captura y la resolución expresada en valor y unidad, como información de georreferenciación mediante latitud y longitud.

En este esquema relacional, el resultado del procesamiento no se modela como una entidad independiente, sino que se incorpora directamente en \textit{Foto} mediante el atributo \texttt{ruta\_trazas}. De este modo, cada registro de imagen incluye la ruta al recurso original, \texttt{ruta\_foto}, y la ruta al artefacto generado tras la inferencia, \texttt{ruta\_trazas}, estableciendo una correspondencia directa entre entrada y salida. Esta decisión simplifica el modelo de datos en la fase actual del proyecto, al asumir un único resultado asociado por imagen procesada, y reduce la necesidad de \textit{joins} para recuperar el resultado, manteniendo al mismo tiempo la trazabilidad necesaria para visualización, descarga y auditoría del flujo de procesamiento. 

En caso de evolución futura hacia múltiples ejecuciones o salidas por imagen, este diseño podría extenderse sin ambigüedad mediante la reintroducción de una entidad específica para resultados o ejecuciones.

\section{Diseño arquitectónico}

\begin{figure}
	\centering
	\includegraphics[width=1.5\linewidth]{img/diagrama_flujo}
	\caption[Diagrama de Flujo de la Aplicación]{}
	\label{fig:diagramaflujo}
\end{figure}


La Figura \ref{fig:diagramaflujo} representa la arquitectura general de la aplicación y las interacciones entre sus distintos componentes, diferenciando claramente el lado cliente y el lado servidor.

El usuario interactúa con la aplicación a través del navegador web, que actúa como intermediario entre la interfaz gráfica y el backend. En el cliente se distinguen dos responsabilidades bien separadas. Por un lado, el HTML renderizado mediante Jinja se encarga de mostrar la estructura de la interfaz, incluyendo botones, imágenes y modales informativos. Por otro lado, la lógica implementada en JavaScript, apoyada en un elemento \textit{canvas}, gestiona la descarga de los resultados en formato JSON y su representación gráfica, sin delegar operaciones de modificación visual al servidor.

El servidor, implementado con Flask, centraliza la lógica de negocio y expone los distintos endpoints a través de un \textit{Blueprint} dedicado a la gestión de trazas. Para mantener el estado de la aplicación entre peticiones HTTP, se utiliza una \textit{session cookie} que almacena información relativa a la imagen activa y a la disponibilidad de resultados calculados. Asimismo, el sistema emplea almacenamiento en disco, dentro del directorio \textit{instance}, tanto para persistir las imágenes subidas por el usuario como para guardar los resultados intermedios en formato JSON.

Este diseño garantiza una separación clara de responsabilidades, evitando que el cliente modifique directamente recursos en el servidor y relegando el procesamiento visual al navegador, lo que mejora la escalabilidad y simplifica la arquitectura.

\subsection{Flujo temporal de interacción entre componentes}

\begin{figure}
	\centering
	\includegraphics[width=1.5\linewidth]{img/diagrama_secuencia}
	\caption[Diagrama de Secuencia de la aplicación]{}
	\label{fig:diagramasecuencia}
\end{figure}

La Figura \ref{fig:diagramasecuencia} describe de forma cronológica el flujo de interacción entre el usuario, el navegador, el servidor Flask y el sistema de almacenamiento, permitiendo comprender el comportamiento dinámico de la aplicación.

El proceso se inicia cuando el usuario selecciona una imagen desde la interfaz. El navegador envía dicha imagen al servidor mediante una petición \textit{POST upload}, tras lo cual Flask la persiste en disco y actualiza el estado de la sesión. A continuación, el servidor responde con una redirección que provoca la recarga de la página principal, mostrando la imagen cargada.

Posteriormente, cuando el usuario solicita el cálculo de trazas, el servidor ejecuta la lógica correspondiente, genera los resultados en formato JSON y los almacena en el sistema de ficheros, actualizando de nuevo la sesión para reflejar el nuevo estado. La respuesta se materializa en una recarga de la interfaz que incluye un modal de confirmación.

Una vez cerrado el modal, el navegador solicita explícitamente los resultados mediante una petición \textit{GET traces}. El servidor lee el fichero JSON previamente generado y lo devuelve al cliente, donde el código JavaScript se encarga de dibujar las trazas sobre el \textit{canvas} y reflejar visualmente el estado de la aplicación.

Finalmente, si el usuario decide eliminar la imagen, se envía una petición del tipo \textit{post} a la ruta \textit{delete} del sistema. El servidor elimina tanto la imagen como los resultados asociados, limpia la información de la sesión y devuelve la aplicación a su estado inicial mediante una nueva recarga de la página.

\section{Diseño web}

En este apartado se presentan los \textit{mockups} de las principales pantallas de la aplicación, con el objetivo de concretar el flujo de interacción previsto y servir de soporte a la especificación funcional. Estas maquetas permiten validar, de forma temprana, tanto la distribución de la interfaz como la secuencia de acciones que guían al usuario a través del \textit{pipeline} de procesamiento, desde la selección de una ortoimagen hasta la obtención y visualización de las trazas detectadas.

\subsubsection{Página principal}

\begin{figure}[tbp]
	\centering
	\includegraphics[
	width=\textwidth,
	trim=0 450 0 0,
	clip,
	page=1
	]{../assets/mockup/Mockup_web.pdf}
	\caption[Mockup de la web: página principal]{Mockup de la web: página principal.}
	\label{fig:mockupweb1}
\end{figure}

La Figura~\ref{fig:mockupweb1} representa el estado inicial de la aplicación, cuando todavía no se ha cargado ninguna imagen. En la parte superior se dispone una barra de acciones que agrupa las operaciones principales del sistema, insertar y borrar imagen, calcular trazas y dibujar trazas, mientras que la zona central actúa como área de trabajo, ofreciendo un contenedor de subida y previsualización. Este diseño prioriza la claridad del flujo, de modo que el usuario identifique desde el primer momento cuál es el punto de entrada del proceso y qué pasos debe ejecutar para pasar de la imagen de entrada a la representación visual del resultado.

\subsubsection{Imagen insertada}

\begin{figure}[tbp]
	\centering
	\includegraphics[
	width=\textwidth,
	trim=0 450 0 0,
	clip,
	page=2
	]{../assets/mockup/Mockup_web.pdf}
	\caption[Mockup de la web: imagen insertada]{Mockup de la web: imagen insertada.}
	\label{fig:mockupweb2}
\end{figure}

El estado inmediatamente posterior a la página principal~\ref{fig:mockupweb1} se alcanza cuando el usuario pulsa \textit{Insertar imagen} y selecciona el fichero que desea analizar. En ese momento, la interfaz pasa a mostrar la imagen cargada, tal y como se aprecia en la Figura~\ref{fig:mockupweb2}. De este modo, el contenedor que en la pantalla inicial presentaba el texto \textit{upload image} se sustituye por la previsualización de la ortoimagen, quedando esta disponible para su inspección en el cliente y para la ejecución posterior del cálculo de trazas.

\subsubsection{Modal de trazas calculadas}

\begin{figure}[tbp]
	\centering
	\includegraphics[
	width=\textwidth,
	trim=0 450 0 0,
	clip,
	page=3
	]{../assets/mockup/Mockup_web.pdf}
	\caption[Mockup de la web: modal de trazas]{Mockup de la web: modal de trazas.}
	\label{fig:mockupweb3}
\end{figure}

Una vez se pulse el botón \textit{Calcular trazas}, aparecerá un modal superpuesto sobre la imagen indicando que el proceso de inferencia ha finalizado y que el siguiente paso consiste en representar el resultado. Para ello, tal y como se muestra en la Figura~\ref{fig:mockupweb3}, el usuario deberá pulsar el botón \textit{Dibujar trazas}.
\subsubsection{Trazas dibujadas}

\begin{figure}[tbp]
	\centering
	\includegraphics[
	width=\textwidth,
	trim=0 450 0 0,
	clip,
	page=4
	]{../assets/mockup/Mockup_web.pdf}
	\caption[Mockup de la web: trazas dibujadas]{Mockup de la web: trazas dibujadas.}
	\label{fig:mockupweb4}
\end{figure}

Tras hacer uso del botón \textit{Dibujar trazas} y, suponiendo que se ha seguido correctamente el flujo previo, se mostrará la ortoimagen original con las trazas superpuestas. De este modo, las sendas detectadas quedan resaltadas y son fácilmente inspeccionables por el usuario, tal y como se aprecia en la Figura~\ref{fig:mockupweb4}.

\subsubsection{Modal de error}

\begin{figure}[tbp]
	\centering
	\includegraphics[
	width=\textwidth,
	trim=0 450 0 0,
	clip,
	page=5
	]{../assets/mockup/Mockup_web.pdf}
	\caption[Mockup de la web: modal de error]{Mockup de la web: modal de error.}
	\label{fig:mockupweb5}
\end{figure}

Si se interrumpe el flujo de la aplicación o se produce algún error durante la ejecución, el sistema mostrará un modal informativo indicando que no ha sido posible completar correctamente el proceso. Este comportamiento permite comunicar la incidencia de forma clara y mantener la interfaz en un estado consistente, tal y como se observa en la Figura~\ref{fig:mockupweb5}.

\subsubsection{Menú de la web}
En este segundo \textit{mockup} se aborda la ampliación funcional orientada al tratamiento geoespacial de la aplicación, incorporando un flujo de trabajo basado en selección de zonas, generación de teselas y gestión de resultados.

\begin{figure}[tbp]
	\centering
	\includegraphics[
	width=\textwidth,
	trim=0 450 0 0,
	clip,
	page=1
	]{../assets/mockup/Mockup_web_pte2.pdf}
	\caption[Mockup de la web: menú]{Mockup de la web: menú.}
	\label{fig:mockupweb6}
\end{figure}

En primer lugar, se introduce un menú de navegación, tal y como se observa en la Figura~\ref{fig:mockupweb6}. Esta incorporación resulta necesaria al evolucionar la aplicación desde una única pantalla hacia un conjunto de vistas diferenciadas, permitiendo el acceso rápido a cada módulo y garantizando una navegación clara entre funcionalidades.

\subsubsection{Visor}

\begin{figure}[tbp]
	\centering
	\includegraphics[
	width=\textwidth,
	trim=0 450 0 0,
	clip,
	page=2
	]{../assets/mockup/Mockup_web_pte2.pdf}
	\caption[Mockup de la web: visor]{Mockup de la web: visor.}
	\label{fig:mockupweb7}
\end{figure}

La siguiente pantalla corresponde al visor cartográfico, mostrado en la Figura~\ref{fig:mockupweb7}. En esta vista se integrará un mapa interactivo mediante \textit{Leaflet}, capaz de cargar ortofotografía del PNOA a niveles de \textit{zoom} adecuados. Una vez alcanzada la escala de trabajo, el usuario seleccionará dos puntos para definir un rectángulo delimitador, entendido como zona de interés. A partir de dicha geometría, el sistema generará un conjunto de teselas correspondientes al recorte definido, asociando cada imagen resultante a la zona seleccionada.

\subsubsection{Colección de imágenes}

\begin{figure}[tbp]
	\centering
	\includegraphics[
	width=\textwidth,
	trim=0 450 0 0,
	clip,
	page=3
	]{../assets/mockup/Mockup_web_pte2.pdf}
	\caption[Mockup de la web: colección de imágenes]{Mockup de la web: colección de imágenes.}
	\label{fig:mockupweb8}
\end{figure}

Una vez generadas las teselas, el usuario podrá acceder a la colección de zonas registradas, representada en la Figura~\ref{fig:mockupweb8}. Esta pantalla organiza en una tabla todas las zonas creadas, mostrando como metadatos informativos las coordenadas de origen y fin del recuadro, junto con un conjunto de acciones asociadas. Entre ellas, se contempla la previsualización de la zona, la descarga de un fichero \texttt{.zip} con las teselas generadas y un indicador de estado que refleje si el cálculo de trazas ya se ha completado para todas las imágenes. Adicionalmente, se incluyen accesos directos a la galería, a la visualización de la zona en el mapa, y una opción de eliminación permanente del registro, junto con un control de paginación para navegar entre entradas.

\subsubsection{Galería de imágenes}

\begin{figure}[tbp]
	\centering
	\includegraphics[
	width=\textwidth,
	trim=0 450 0 0,
	clip,
	page=4
	]{../assets/mockup/Mockup_web_pte2.pdf}
	\caption[Mockup de la web: galería de imágenes]{Mockup de la web: galería de imágenes.}
	\label{fig:mockupweb9}
\end{figure}

Al acceder a la galería, se presenta un listado de las imágenes asociadas a la zona seleccionada, tal y como se observa en la Figura~\ref{fig:mockupweb9}. Cada elemento incluye su identificación y metadatos básicos, además de un indicador de progreso que señala si las trazas han sido calculadas. Este cálculo se plantea como un proceso automático, de modo que el usuario pueda supervisar el estado sin intervención adicional.

\subsubsection{Imagen resaltada}

\begin{figure}[tbp]
	\centering
	\includegraphics[
	width=\textwidth,
	trim=0 450 0 0,
	clip,
	page=5
	]{../assets/mockup/Mockup_web_pte2.pdf}
	\caption[Mockup de la web: imagen resaltada]{Mockup de la web: imagen resaltada.}
	\label{fig:mockupweb10}
\end{figure}

Por último, la Figura~\ref{fig:mockupweb10} muestra la vista de detalle de una imagen individual. En esta pantalla se habilita la consulta de la imagen a mayor resolución y la inspección de sus características, incorporando además acciones explícitas sobre el procesado. En particular, se incluye un botón para dibujar las trazas, únicamente cuando estas hayan sido calculadas previamente, y un botón para recalcularlas en caso de fallo o inconsistencia durante la ejecución automática.

\subsubsection{Registro}
En este tercer \textit{mockup} se aborda la incorporación de la lógica de usuarios en la aplicación, introduciendo un flujo de autenticación con registro e inicio de sesión, una gestión básica del perfil y un conjunto de vistas restringidas a usuarios con rol de administrador. Esta ampliación permite habilitar funcionalidades que requieren persistencia y control de permisos, manteniendo a su vez un modo de uso público con capacidades limitadas.

\begin{figure}[tbp]
	\centering
	\includegraphics[
	width=\textwidth,
	trim=0 450 0 0,
	clip,
	page=1
	]{../assets/mockup/Mockup_web_usuarios.pdf}
	\caption[Mockup de la web: registro]{Mockup de la web: registro.}
	\label{fig:mockupweb11}
\end{figure}

La Figura~\ref{fig:mockupweb11} presenta la pantalla de registro, concebida como punto de entrada para la creación de una cuenta. La interfaz organiza el formulario en dos columnas para separar datos de identificación y contacto, como usuario, correo electrónico y un campo teléfono opcional, de los campos de credenciales, como contraseña y repetición de la misma, reduciendo errores de introducción.

Tras completar los campos, el usuario confirma la operación mediante el botón \textit{Registrarse}, y se incluye un enlace alternativo para acceder directamente a la pantalla de inicio de sesión cuando ya se dispone de una cuenta.

\subsection{Inicio de Sesión}

\begin{figure}[tbp]
	\centering
	\includegraphics[
	width=\textwidth,
	trim=0 450 0 0,
	clip,
	page=2
	]{../assets/mockup/Mockup_web_usuarios.pdf}
	\caption[Mockup de la web: log in]{Mockup de la web: log in.}
	\label{fig:mockupweb12}
\end{figure}

La Figura~\ref{fig:mockupweb12} muestra la pantalla de inicio de sesión, destinada a autenticar al usuario y habilitar las funcionalidades asociadas a su rol. La interfaz se divide en dos secciones: a la izquierda se presenta una descripción breve del propósito de la aplicación y una diferenciación entre el modo de uso sin autenticación, orientado al procesamiento de imágenes individuales, y el modo autenticado, que habilita herramientas cartográficas y gestión persistente de colecciones.

A la derecha se dispone el formulario de acceso, compuesto por los campos de usuario y contraseña y el botón \textit{Iniciar Sesión}, junto con un enlace directo a la pantalla de registro para usuarios que todavía no dispongan de cuenta.

\subsection{Menú de usuarios}

\begin{figure}[tbp]
	\centering
	\includegraphics[
	width=\textwidth,
	trim=0 450 0 0,
	clip,
	page=3
	]{../assets/mockup/Mockup_web_usuarios.pdf}
	\caption[Mockup de la web: Menú de usuarios]{Mockup de la web: Menú de usuarios.}
	\label{fig:mockupweb13}
\end{figure}

La Figura~\ref{fig:mockupweb13} muestra el menú de navegación una vez el usuario ha iniciado sesión. Su estructura es análoga a la del menú introducido en el \textit{mockup} de la parte geoespacial, manteniendo las entradas principales de la aplicación, como \textit{Home}, \textit{Visor} y \textit{Colección de imágenes}. No obstante, en esta versión se incorporan las nuevas pantallas asociadas a la gestión de usuarios, incluyendo el acceso al perfil y la opción de cierre de sesión.

Adicionalmente, se añade un \textit{panel del administrador} que agrupa funcionalidades de mantenimiento internas, como la gestión de usuarios y la gestión del modelo, el cual únicamente se muestra cuando el rol autenticado corresponde a administrador, de modo que el menú se adaptará dinámicamente a los permisos disponibles.

\subsection{Gestión de usuarios}

\begin{figure}[tbp]
	\centering
	\includegraphics[
	width=\textwidth,
	trim=0 450 0 0,
	clip,
	page=4
	]{../assets/mockup/Mockup_web_usuarios.pdf}
	\caption[Mockup de la web: Gestión de usuarios]{Mockup de la web: Gestión de usuarios.}
	\label{fig:mockupweb14}
\end{figure}

La Figura~\ref{fig:mockupweb14} muestra la pantalla de \textit{Gestión de usuarios}, accesible exclusivamente para el rol de administrador. Esta vista centraliza la supervisión y administración de cuentas registradas, presentando un resumen superior con métricas agregadas, como el número total de usuarios, la cantidad de administradores y el número de usuarios regulares, y una tabla principal con los registros disponibles.

En dicha tabla se incluyen campos identificativos y de contacto, junto con el rol asignado y la fecha de registro, incorporando además mecanismos habituales de explotación de listados, como búsqueda, paginación y exportación de resultados. Finalmente, se contempla una acción de eliminación por usuario, destinada a operaciones de mantenimiento y control del sistema, manteniendo la coherencia con el modelo de permisos, de modo que solo el administrador pueda ejecutar modificaciones sobre el conjunto global de cuentas.

\subsection{Gestión de \textit{folds}}

\begin{figure}[tbp]
	\centering
	\includegraphics[
	width=\textwidth,
	trim=0 450 0 0,
	clip,
	page=5
	]{../assets/mockup/Mockup_web_usuarios.pdf}
	\caption[Mockup de la web: Gestión de folds]{Mockup de la web: Gestión de folds.}
	\label{fig:mockupweb15}
\end{figure}

La Figura~\ref{fig:mockupweb15} muestra la pantalla de \textit{Gestión de folds}, concebida como una funcionalidad exclusiva del rol de administrador para supervisar y ajustar el modelo de inferencia empleado por el sistema. En esta vista se incluye un selector que lista los distintos \textit{folds} disponibles e indica explícitamente cuál se encuentra activo, permitiendo seleccionar un \textit{fold} alternativo para su comparación.

A continuación, se habilita un área de carga de imagen destinada a pruebas controladas, de modo que la misma entrada pueda procesarse con el \textit{fold} activo y con el \textit{fold} seleccionado, mostrando ambos resultados en paralelo para facilitar una inspección cualitativa de diferencias de segmentación. Finalmente, se incorpora un botón para establecer el \textit{fold} seleccionado como predeterminado, actualizando la configuración del sistema de forma persistente para futuras ejecuciones.

\subsection{Gestión de cuenta}

\begin{figure}[tbp]
	\centering
	\includegraphics[
	width=\textwidth,
	trim=0 450 0 0,
	clip,
	page=6
	]{../assets/mockup/Mockup_web_usuarios.pdf}
	\caption[Mockup de la web: Gestión de cuenta]{Mockup de la web: Gestión de cuenta.}
	\label{fig:mockupweb16}
\end{figure}

La Figura~\ref{fig:mockupweb16} muestra la pantalla de \textit{Gestión de cuenta}, destinada a centralizar la información básica del usuario autenticado y a ofrecer accesos directos a las funcionalidades principales asociadas a su sesión. En esta vista se presentan los datos de perfil, incluyendo nombre de usuario, correo electrónico, teléfono y fecha de registro, junto con una acción para \textit{Editar información}, orientada a la actualización de los campos permitidos.

Asimismo, se incorporan accesos rápidos al visor y a la colección de imágenes, con el objetivo de reducir fricción en la navegación y facilitar el retorno a los módulos de trabajo. Finalmente, se incluye la opción de \textit{Cerrar sesión}, que invalida la sesión activa y devuelve la aplicación al modo de acceso no autenticado.

\section{Diseño procedimental}





